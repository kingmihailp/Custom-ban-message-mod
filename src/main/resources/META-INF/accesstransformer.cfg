# Custom Ban Messages — Access Transformers
#
# Exposes the protected Connection field on ServerCommonPacketListenerImpl so
# we can read the remote SocketAddress when kicking players on IP-ban.
#
# Format: <modifier> <fully.qualified.ClassName> <fieldName>
public net.minecraft.server.network.ServerCommonPacketListenerImpl connection
